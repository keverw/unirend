// Flip to true to enable the error demo routes.
// Run via `bun run ssr:serve:dev` (dev SSR server) or `bun run ssr:build-and-serve:dev` (built).
// Useful for demoing error behavior and designing/previewing error pages.
export const ENABLE_TEST_ROUTES = true;
